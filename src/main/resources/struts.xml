<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
"-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
"http://struts.apache.org/dtds/struts-2.0.dtd">
 
<struts>
    <package name="default" namespace="/" extends="convention-default">
        <interceptors>    
            <interceptor name="loginInterceptor" class="com.cgoettert.tickets.interfaces.web.struts.interceptor.LoginInterceptor" />
            <interceptor-stack name="customStack">
                <interceptor-ref name="loginInterceptor"/>
                <interceptor-ref name="defaultStack" />
            </interceptor-stack>
        </interceptors>
        <default-interceptor-ref name="customStack"/>
    </package>
    
    <constant name="struts.devMode" value="true" />    
    <constant name="actionPackages" value="com.cgoettert.tickets.interfaces.web.struts" />
    <constant name="struts.convention.exclude.parentClassLoader" value="true" />
    <constant name="struts.convention.action.fileProtocols" value="jar,vfsfile,vfszip" />
    <constant name="struts.convention.classes.reload" value="true" />
    <constant name="struts.convention.default.parent.package" value="default" />

</struts>